spring:
 application:
  name: api

 datasource:
  url: jdbc:postgresql://filedata-db.c7qsicc2w4g8.sa-east-1.rds.amazonaws.com:5432/postgres
  username: postgres
  password: ${DB_PASSWORD}

 jpa:
  hibernate:
   ddl-auto: update
  show-sql: true
  properties:
   hibernate:
    dialect: org.hibernate.dialect.PostgreSQLDialect

aws:
 bucket: file-uploadace0777