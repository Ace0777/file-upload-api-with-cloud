spring:
 application:
  name: api

 datasource:
  url: jdbc:postgresql://localhost:5432/fileData
  username: postgres
  password: password

 jpa:
  hibernate:
   ddl-auto: update
  show-sql: true
  properties:
    hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect